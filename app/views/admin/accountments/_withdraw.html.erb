<tr class="table td primary">
  <td><%= link_to withdraw.id, admin_withdraw_path(currency: withdraw.currency_id, id: withdraw.id) %></td>
  <td><%= link_to Member.find_by_id(withdraw.member_id).email, url_for([:admin, withdraw.member]), target: '_blank' %></td>
  <td><%= withdraw.currency_id %></td>
  <td><%= withdraw.amount %></td>
  <td><%= withdraw.fee %></td>
  <td><%= withdraw.txid %></td>
  <td><%= withdraw.aasm_state %></td>
  <th><%= withdraw.sum %></th>
  <td><%= withdraw.rid %></td>
  <td><%= withdraw.created_at %></td>
</tr>